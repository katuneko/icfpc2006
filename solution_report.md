# ICFPC 2006 解法レポート

この文書は、ここまでに解いてきた問題を「何をしたか」ではなく「なぜその解き方が本質的だったか」を中心に整理したものです。詳細なコマンド列、入出力ログ、個別の試行錯誤は `walkthrough.md` と各 `*_input.txt` / `volume9_*.txt` に残し、本稿では解法の構造と再利用できる洞察をまとめます。

## 現在の到達点

現在の CV weight は 5750、権限は Full Administrator です。内訳は次の通りです。

| 分野 | 点 |
| --- | ---: |
| INTRO | 235 |
| BASIC | 100 |
| ADVTR | 810 |
| ANTWO | 860 |
| BLNCE | 1087 |
| BLACK | 1000 |
| CIRCS | 1329 |
| ADVIS | 329 |
| 合計 | 5750 |

このスコアは単なる「解けた問題数」ではなく、いくつかの分野ではプログラム長、面積、アドバイス規則数などの品質が直接点数に反映されます。特に CIRCS と ADVIS は、正解を出した後も表現を洗練する余地が大きい分野です。

## 横断的な洞察

このパズル全体では、表面上は別々の言語やゲームに見えるものが、実際には「小さな意味論を正確に再現し、公式 verifier を局所的な oracle として使う」問題として揃っています。UM、Adventure、Antomaton、Balance、2D、O'Cult はどれも、最初に自然言語の説明を信じ切るよりも、実行環境から観測した意味論を手元のモデルに移し、そのモデルで探索・圧縮・検証を回す方が強力でした。

もう一つの共通点は、正解対象の状態空間を直接総当たりしないことです。Antomaton では信号到着時刻や局所盤面を切り出し、Black Knots ではトークンの経路と隣接 swap に分解し、Balance では短い命令列マクロへ落とし、2D ではモジュール単位で参照実装と一致させました。大きな探索を、小さな不変量と局所検証に分割することが前進の鍵でした。

最後に、採点器は常に「仕様どおりの美しい解」だけを要求しているわけではありません。UMIX のファイル、認証、実行中のプログラム、壊れたオブジェクトの内部構造など、実行環境そのものが問題の一部です。したがって、何が提出文字列として評価され、何が実行時の副作用として扱われるかを分離することも重要でした。

## UM と UMIX

### 本質的な洞察

UM はこのコンテストの基盤ですが、単に仕様どおりの仮想機械を作るだけでは足りません。重要なのは、UMIX が「対話型 OS であり、後続のすべての puzzle への配送路でもある」ことです。したがって、インタプリタは速く正確であるだけでなく、ログを再生でき、長い対話を入力ファイルとして固定できる必要がありました。

### 解法

Rust 製の `um.rs` で UM を実装し、`codex.umz` の展開、`volume9.um` の実行、UMIX へのログイン、プログラム実行を再現可能にしました。`*_input.txt` に対話列を残し、`./um volume9.um < input > output` 形式で同じ状態を何度も再現できるようにしたことが、以後のすべての検証の土台です。

INTRO 系では guest ログイン、`umodem`、`ucc` などを通じて基本コードを回収しました。ここで得た教訓は、UMIX のコマンド群は単なる雰囲気ではなく、採点コード、ヒント、別形式のプログラム、ユーザアカウントを運ぶ実体だということです。

## Adventure

### 本質的な洞察

Adventure はテキストアドベンチャーではなく、壊れた値を型付きデータ構造として操作する puzzle でした。表面的には「アイテムを拾って組み合わせる」ゲームですが、実際には `Broken { condition, missing }` の入れ子構造を観察し、どの層がどの操作で消費されるかを理解する必要があります。

特に重要だったのは、`combine` が壊れた構造の全階層を一気に直すのではなく、直近の missing layer だけを消費することです。また `take` は山の最上部を見る、`incinerate` は削除ではなく Trash Heap へ移す、という実装上の事実も解法を左右しました。ゲーム内の説明より、goggles で見える XML / S-expression / ML 表現が真の仕様でした。

### 解法

Howie のパスワードは QBasic プログラムを解析して `xyzzy` と特定しました。`/etc/passwd` やメールから `yang / U+262F`、`hmonk / COMEFROM` などのアカウント情報を回収し、UMIX 側の探索範囲を広げました。

Repair 系では、まず downloader で `gc.rml` を取得し、uploader で Adventure エンジンを差し替えました。インベントリ上限を 6 から 666 に拡張し、`drop` と teleport 用の `speak trash/home/museum` を追加したことで、巨大な壊れオブジェクト群を実験可能にしました。この改造はズルというより、以後の構造観察を可能にするデバッガ導入でした。

Museum では blueprint、Censory Engine、manifesto の連鎖が中心です。`knr / X3.159-1989` で得た crowbar により Censory Engine を破壊し、赤塗りされた情報を復元しました。ここで Adventure が単体ゲームではなく、UMIX アカウント、ファイル、別 puzzle の入口と結びついていることが明確になりました。

Sequent sequel の各部品は、単に pristine な同名部品を集めるだけでは直りません。Race car、pousse game、package robot、finite-state machine、codex では、missing が要求する壊れ方に合わせた部分修復状態が必要でした。とくに robber では、`amber X-4832-TFT` を特定の段階まで修復し、それを `cardinal B-1403-YIC` に組み込み、さらに `carmine robber` に入れる、という中間状態の一致が本質でした。`T-9247-OCM` は missing 記述に現れるが実体としては手元にない red herring でした。

### 教訓

Adventure で強かった手法は、オブジェクト名ではなく構造で見ることです。壊れた対象を「名前つきアイテム」として扱うと組み合わせ爆発しますが、missing tree と現在の破損状態の差分として扱うと、必要な中間状態が見えるようになります。

## Antomaton

### 本質的な洞察

Antomaton の核心は、蟻のプログラム探索ではなく、成功条件と wildcard の意味論を正しく掴むことでした。成功は「蟻が食料へ向いている」状態で、方角は回転対称です。食料の北側に立つ必要があるわけではありません。この一点だけで、探索の評価関数とゴール判定が大きく変わりました。

また `*` は一種類ではありません。盤面の wildcard、プログラム表の wildcard、固定された空白として扱うべき wildcard が混在します。グリッド wildcard に食料を置くような自然な緩和は verifier では通らず、`CELL_WILD=-1` を通常セルの添字として回してしまうと、回転表の末尾に誤って吸い込まれるような偽解が出ます。探索前に意味論を固めることが最重要でした。

### 解法

`ant_solver.py` と関連スクリプトで公式ルールのシミュレータを作り、固定領域、wildcard 初期配置、プログラム表を分けて探索しました。

P3/P4 では、固定領域への注入が不十分だと本来の floor が失われて偽解になります。残る `x20` floor を含めて固定領域を正確に移植し、P4 の構造を P3 に移すことで、`NWSWWSN` / `ENNNEEW` と delay-line ants による時刻合わせが成立しました。

P8/P11 では、先に固定 entry event schedule を決め、次に wildcard 初期状態をローカルサーチしました。P8 は 16 個の wildcard cell をすべて蟻として使う解、P11 は `NSSENWN` / `SNSEWES` のプログラム対が鍵でした。

P9/P10 は `ant_local_search.py` で、盤面 wildcard とプログラム slot を同時に変異させ、具体シミュレーション上の近さで評価しました。P9 は一度得た候補を再開点として使い、near-target や target-turn support を評価に入れることで、最後の同期を詰められました。

最後に残った P6/P7 は、単なる食料距離ではなく「指定時刻に指定向きの信号を出す」問題として解けました。P6 は上段で東向き signal を作ることが本質で、`--p6-focus` に p1 直進条件、右隣への同時流入ペナルティ、p2 entry-turn の exact support を入れたことで、`puzzle6_solution.ant` が step 29 で成功しました。出版は `ANTWO.006=70@999999|bf9170fc018b77e956cb50371a58b3d` です。

P7 は固定機械の 4 相周期を理解することが突破口でした。中央井戸の下端 `(6,15)^` / `(8,15)^` に p1=N の変動蟻を入れられれば、その後は直進だけで固定蟻群に受け渡されて食料へ到達します。`--p7-focus` で外周 apron から中央井戸へ入る状態を評価し、`puzzle7_solution.ant` が step 27 で成功しました。出版は `ANTWO.007=30@999999|73be091344104ac048550d8dcfd324c` です。

これで Antomaton は Puzzle 1..15 の全出版を回収しました。

## Black Knots

### 本質的な洞察

Black Knots は、一見すると黒い結び目の図形変形ですが、本質はモデル仕様上の隣接 swap 列です。`|><` の操作を、トークンの並び替えと行・列の隣接交換として捉えると、図形問題から permutation 問題へ落とせます。

この変換により、解けるかどうかの判定には反転数や parity が効きます。探索の前に parity で不可能枝を落とせること、さらに serial construction で多数のモデルを機械的に組めることが重要でした。

### 解法

`black_knots_solver.py` に inversion parity pruning と serial construction を入れ、000/010/030/040/050/100/200/300/500 を構成しました。行や列を一つずつ確定していく構成法により、巨大な swap 空間を直接探索せずに済みました。

020 は通常の serial construction だけでは詰まり、Exact Cover / DLX として解き直しました。トークンごとの経路を固定し、そこから必要な row swap を復元することで、局所的には選択問題、全体としては一貫した swap 列に落とせました。

400 は非常に大きく、行数 69963 のような巨大な対象でした。ここでは serial construction と最小 swap 列を基本に、残る差分を追加 swap loop で対にして潰す方向が効きました。また UMIX / verifier の実行挙動も解析対象となり、提出文字列の検証境界を理解すること自体が puzzle の一部でした。

### 教訓

Black Knots の解法は、「図として難しいものを、可換でない局所操作列として見る」ことです。モデルをそのまま絵で追うのではなく、操作の不変量と token identity を明示した時点で、探索問題として扱えるようになりました。

## Balance

### 本質的な洞察

Balance は hex bytecode の小さな機械語 puzzle です。ここで重要なのは、命令を一つずつ意味で考えるより、短い idiom として扱うことでした。レジスタ移動、ポインタ移動、カウンタ更新、停止値生成をマクロのように見られるようになると、プログラム長を抑えながら目的動作を組み立てられます。

もう一つの洞察は、採点側が最終状態の関係を検証する問題では、プログラムが美しく決定的である必要は必ずしもないことです。`multmem` では探索・検証により、確率的または非直観的な短いプログラムでも verifier を通ることが確認できました。

### 解法

`stop1` / `stop127` / `stop128` では、停止判定の `SCIENCE 0` を番兵として使い、2 bytes で目的セルに来た時だけ止める形まで短縮しました。`stop1` は `PHYSICS 1; SCIENCE 0`、`stop128` は `PHYSICS -16; SCIENCE 0`、`stop127` は `SCIENCE 0; PHYSICS 1` です。

`fillmem` では、最初に preloop でレジスタの意味を整え、main loop へ入ってから同じ骨格を使い回しました。loop1 は zero-fill と counter decrement を担当し、`MATH d0,s3,s1` と `dR0++` の組み合わせで範囲を進めます。loop2 は pre から main への register relocation 後に同じ構造を再利用し、`[i,j)` を `a` で埋めます。

`copyreg` は既存の確率通過型31 bytes解から段階的に短縮し、最終的に12 bytesの `copyreg12_candidate.bal` まで到達しました。採用版はローカル全列挙で196/255ケース成立し、255/255ケースでhaltします。公式 `certify copyreg` の反復実行で `BLNCE.CRE=190` を取得できました。11 bytes以下は未探索ですが、12 bytes近傍でも候補間の成功率差が大きく、今後も確率解探索として改善余地があります。`copymem`、`swapmem`、`swapreg`、`swapreg2`、`addmem`、`addmem2`、`clearreg` なども、最終的に命令列の個別発明ではなく、小さなデータ移動 idiom の組み合わせとして整理できました。

### 教訓

Balance の高得点化では、命令列を短くするより先に「どの状態量をどこに置けばループが自然に縮むか」を考えるべきです。レジスタ配置の選び方が、その後の全命令数を支配します。

## 2D / CIRCS

### 本質的な洞察

2D は図形言語ですが、実際には代数的データ型をワイヤで流す純粋関数系です。値は `()`、pair、`Inl`、`Inr` で表され、箱とワイヤと module 呼び出しで関数を構成します。したがって、図を描く前に各 module の入出力型と参照実装を固定することが重要でした。

`#` の意味を crossover として扱うこと、strict wires で未接続や曖昧接続を早期に落とすことも大きな安定化要因でした。見た目でつながっているように見える図でも、parser と verifier の見方が一致しなければ提出は通りません。

### 解法

`mult` と `rev` は、まず strict wire として正しい構造に直し、公式 `verify` で確認しました。`rev` は後に右端を crop した `rev_compact.2d` も試しましたが、提出コードは変わらずスコア改善はありませんでした。

`raytrace` は最も大きな成果です。Python の least-fixed-point 参照実装を作り、2D 側では intensity の max/min、`F` table、Towards/Away、`awayv`、`away`、`update`、`rt` といった module に分割しました。各 module を参照実装と突き合わせ、全体として verifier を通した後、配置を詰め直して `raytrace_2_repack.2d` を作りました。

この repack は意味を変えずに module 配置を整理するもので、公式 `verify raytrace` の Program area を 13340 まで下げ、提出コードを `CIRCS.RAY=1264@999999|6ef053487b3deb2307b0f34255390bd` に改善しました。さらに同じ module 群の再配置で `raytrace_2_repack2.2d` を作り、Program area は 13299 まで下がりました。続いて `A` module 内の `All` 出力を branch payload から再構成する `send[(Inr Inr W,E)]` に短縮し、`raytrace_2_repack3.2d` で Program area を 13206 まで下げました。ただし出版コードは `1264` のままで CV weight は増えませんでした。後続の Balance 短縮まで反映した現在の CV weight は 5750 です。

`ocult` については未完成ですが、重要な意味論は見えています。`verify ocult` が期待するのは生の次項ではなく、`Inl ()` で「適用なし」、`Inr <term>` で「一回 rewrite した項」を表す Option です。この理解がないと、内部の rewrite が正しくても verifier の型に合いません。

### 高スコア化への示唆

CIRCS は、現在解けている問題だけでも得点を伸ばせる領域です。`raytrace_2_repack.2d` の改善が示した通り、論理を変えずに配置と module 境界を洗練するだけで点が動きます。ただし `raytrace_2_repack3.2d` の 13206 area でも出版点が変わらなかったため、次に見るべきなのは、単なる外形 packing よりも、再帰 module の分割、`X`/`Q` の右端経路短縮、または採点上限の確認です。

## O'Cult Advice / ADVIS

### 本質的な洞察

O'Cult advice のスコアは、解けるかどうかだけでなく advice の簡潔さに依存します。ここでは「規則を増やして全ケースを覆う」よりも、対象言語の評価構造を見抜いて、少数の規則で広い形を吸収することが重要でした。

Arithmetic では、個別の演算を展開しすぎるとサイズが増えます。XML では、局所的なタグ処理と列の再結合を同じ規則に詰めると複雑化します。どちらも、評価の共通骨格を別規則へ切り出すことで短くなりました。

### 解法

Arithmetic は `arith28.adv` で、CPS の `Eval`/`Done` を捨てて `Compute` 自体を評価関数にし、`AddF`/`MulF` に左辺だけを正規化して渡す形へ短縮しました。rule 7、size 82 の形で `ADVIS.ARH=169@999999|ac21e73f0f47d168d1c2ea1b62c31fa` を得ました。

XML は `xml28.adv` で、Seq recombination を global rule として分離した骨格を保ちつつ、`Tag q` を constructor continuation として `Ins` に渡す形へ短縮しました。`Eval (t d)` と `Ins t k (t d)` が効き、rule 13、size 210 の形で `ADVIS.XML=160@999999|1868d2782cd1eb75239c345b2bb93a2` を得ています。出版点は `xml17.adv` の size 214 と同じでした。

### 教訓

ADVIS では、テストを通す規則を作った時点ではまだ半分です。その後、どの規則が本当に意味論の核で、どの規則がたまたま特定入力を処理しているだけかを分ける必要があります。短縮は、行数削減ではなく抽象化の発見でした。特に O'Cult の「左右の match 数が同じなら適用しない」規則を避けるため、ARH では同じ形の未評価部分式を左右へ同時に置かず、乗算は `x*y` を先に `Compute` してから `+ y` する向きにしました。XML では `Merge` と `Ins` 継続の境界を崩しすぎないことが重要でした。`xml28.adv` では constructor continuation 化で安全に 4 size 縮みましたが、`Merge (t a) k (t b)` まで一般化すると `Seq` も誤って同一 constructor と見なされます。late global cleanup 系の試行は固定テストを通っても公式内部テストで未正規形を残しました。

## Accounts / Administration

### 本質的な洞察

UMIX のアカウントは報酬ではなく、探索空間の分岐です。各ユーザの home、mail、実行ファイル、パーミッションが別々の puzzle の入口になっています。したがって、アカウント情報は見つけた時点で一覧化し、どの puzzle と接続しているかを管理する必要がありました。

### 解法

主要なアカウントとして、`gardener / mathemantica`、`ohmega / bidirectional`、`bbarker / plinko`、`knr / X3.159-1989`、`ftd / falderal90`、`root / c@Rn3g!e` を得ました。

`ftd` では `icfp.exe` が CV を計算し、提出コードの統合検証を担っています。`ml19100.exe` からは、例外機能が完全実装されていない状態でも `BASIC.MLC` を得られました。`root` では `lost+found` の壊れたファイルを `recover` できることが分かりましたが、現時点では新しい提出コードにはつながっていません。

### 教訓

この層では、ファイルシステムそのものが puzzle board です。メール、実行ファイル、壊れたファイル、権限、パスワードを「メタ情報」として流さず、提出コードへつながる依存グラフとして扱うことが重要でした。

## 現在の未解と次の攻め筋

Antomaton は Puzzle 1..15 の見えている問題をすべて解き切りました。今後この分野で伸ばすなら、未解探索ではなく、解の簡約、step 数短縮、または探索器の一般化が主な方向です。

CIRCS では `ocult` が未完成です。まず verifier の Option 型期待に合わせた一歩 rewrite 器を module 単位で完成させるべきです。一方、既に解けている `raytrace` はさらに面積圧縮の余地がありますが、最新の内部短縮では 13206 area でも出版点が変わらなかったため、次の改善にはより大きい構造削減か、RAY の採点上限に関する見極めが必要です。

ADVIS は XML 側にまだ短縮余地があります。ただし 210 から次の出版点に進むには、`Merge` の同一タグ判定を `Seq` に誤爆させずに短くする必要があります。今後は failed variant を捨てるのではなく、どの規則が失敗を生んだかを evaluator trace と対応づけることで、意味を保ったまま規則を統合できる可能性があります。

Adventure / root 周辺は、未使用の recovered file や権限差分をもう一度棚卸しする価値があります。ここは「新しいコードが隠れている」よりも、「既存 puzzle を解くための道具やヒントがまだ残っている」可能性が高いです。

## まとめ

ここまでの進捗は、個別 puzzle の偶然の突破ではなく、共通する解法パターンに支えられています。

1. 公式実行環境を観測し、手元に小さな意味論モデルを作る。
2. モデルと verifier の差分をログで固定し、再現可能な入力にする。
3. 状態空間を不変量、時刻、型、token path、module に分解する。
4. 正解後に、採点関数が見ている品質指標を狙って圧縮する。

この流れは、CIRCS ocult、CIRCS の面積最適化、ADVIS の短縮、Adventure / root 周辺の棚卸しにもそのまま使えます。特に次の大きな伸びしろは、残る未完成分野を一つ増やすことと、既解の表現をさらに磨いて採点指標を下げることの両方にあります。
