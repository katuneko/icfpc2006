hmonk
COMEFROM
run /bin/umodem arith28.adv EOF
Compute (Add x y) => AddF (Compute x) y;
Compute (Mult x y) => MulF (Compute x) y;
Compute v => v;

AddF Z y => Compute y;
AddF (S x) y => S (AddF x y);

MulF Z y => Z;
MulF (S x) y => AddF (Compute (Mult x y)) y;

.
EOF
advise arith arith28.adv
