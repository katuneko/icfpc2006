hmonk
COMEFROM
run /bin/umodem xml26.adv EOF
{ O'Cult XML SNF normalizer, late Emph/Bold order cleanup attempt }

SNF x => Eval x Done;

Seq (Seq a b) y => Seq a (Seq b y);

Eval (Seq x y) k => Eval x (KSeq y k);
Eval (Tag q d) k => Eval d (Ins q k);
Eval v k => k v;

(KSeq y k) v => Eval y (Merge v k);

Merge (Tag q a) k (Tag q b) => Eval (Seq a b) (Ins q k);
Merge a k b => k (Seq a b);

Ins q k (Tag q d) => k (Tag q d);
Ins Maj k (Tag q d) => Ins Maj (Ins q k) d;
Ins q k d => k (Tag q d);

Tag Emph (Tag Bold d) => Tag Bold (Tag Emph d);

Done v => v;

. { end }
EOF
advise xml xml26.adv
